/**
 * task structure : 
 * 
 * @param {*} task 
 * task.taskType (int 1-4)
 * task.taskContent (str obj)
 * @returns 
 */
export const FirstTaskSession = (task) => {
    //FirstTaskContent
    return (
        <>
        
        </>
    )
}