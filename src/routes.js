const REGISTER = "/register";
const LOGIN = "/login";
const HOME = "/home";
const ACCOUNT = "/account";
const TASK = "/tasks";
const routes = {
    REGISTER,
    LOGIN,
    HOME,
    ACCOUNT,
    TASK
};

export default routes