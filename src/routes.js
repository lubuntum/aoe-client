const REGISTER = "/register";
const LOGIN = "/login";
const HOME = "/home";
const ACCOUNT = "/account";
const TASK = "/tasks";
const LESSON_SESSION = "/session"
const routes = {
    REGISTER,
    LOGIN,
    HOME,
    ACCOUNT,
    TASK,
    LESSON_SESSION
};

export default routes